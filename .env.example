# ---- Supabase (required) ----
# Project URL and publishable (anon) key: Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-OR-PUBLISHABLE-KEY

# Postgres connection string for the server and the sync pipeline.
# Dashboard → Connect → "Session pooler" (port 5432) or "Transaction pooler" (6543).
# Contains the database password — keep it server-side only.
SUPABASE_DB_URL=postgresql://postgres.YOUR-PROJECT-REF:YOUR-DB-PASSWORD@aws-1-us-east-1.pooler.supabase.com:5432/postgres

# ---- Optional ----
# Shared secret that lets a scheduler (Vercel Cron / GitHub Actions) call
# POST /api/sync without a user session.
# CRON_SECRET=some-long-random-string
